# macrosh
calling macrosh in the shell launches the macro shell

to print from the macro shell one must type:
printf %s 'printf %s hello'

#fasfu
usage: fastfu < my_c_program.c

to type an ordinary line of c, write it normally

to type a line of fastfu, prepend it with fastfu 

example:
fastfu free x
